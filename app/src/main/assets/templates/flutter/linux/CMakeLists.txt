cmake_minimum_required(VERSION 3.13)
project({{PROJECT_NAME_LOWER}} LANGUAGES CXX)
set(BINARY_NAME "{{PROJECT_NAME_LOWER}}")
cmake_policy(SET CMP0063 NEW)
set(CMAKE_INSTALL_RPATH "$ORIGIN/lib")
include(${FLUTTER_MANAGED_DIR}/cmake/flutter.cmake)
flutter_get_project_settings()
add_subdirectory(${FLUTTER_MANAGED_DIR} managed_flutter)
add_executable(${BINARY_NAME} "main.cc")
target_link_libraries(${BINARY_NAME} PRIVATE flutter)
